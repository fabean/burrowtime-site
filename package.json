{
  "name": "burrowtime-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "The BurrowTime product site and documentation.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:sites": "OPENAI_SITES=1 astro build && node scripts/prepare-sites-build.mjs",
    "check": "astro check",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.2"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@openai/sites-vite-plugin": "^0.2.0",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "rolldown": "1.2.7"
  }
}
